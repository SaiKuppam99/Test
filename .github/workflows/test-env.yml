name: test env

on:
  workflow_dispatch:

jobs:
  test-env:
    runs-on: ubuntu-latest
    environment: test

    steps:
    - name: test env values
      run: |
        echo "Today is ${{ vars.DAY }}"
      
